import React from 'react'

const IconBtn = ({
    text,
    onClick,
    children,
    disable,
    outline=false,
    customClasses,
    type
}) => {
  return (
    <button>
       {
        
       }
    </button>
  )
}

export default IconBtn
