import React from 'react'

const IconBtn = ({
    text,
    onClick,
    children,
    disable,
    outline=false,
    customClasses,
    type
}) => {
  return (
    <div>
      
    </div>
  )
}

export default IconBtn
